// Generated by CoffeeScript 1.10.0
(function() {
  var deflate, inflate;

  deflate = function(array, headers, options) {
    var header, i, item, j, len, len1, res, thisItem;
    res = {
      headers: headers,
      items: []
    };
    for (i = 0, len = array.length; i < len; i++) {
      item = array[i];
      thisItem = [];
      for (j = 0, len1 = headers.length; j < len1; j++) {
        header = headers[j];
        if (item[header] != null) {
          thisItem.push(item[header]);
        } else {
          thisItem.push(null);
        }
      }
      res.items.push(thisItem);
    }
    return res;
  };

  inflate = function(object, options) {
    var i, index, item, j, len, len1, ref, result, thisItem, value;
    result = [];
    if ((object.headers != null) && (object.items != null)) {
      ref = object.items;
      for (i = 0, len = ref.length; i < len; i++) {
        item = ref[i];
        thisItem = {};
        for (index = j = 0, len1 = item.length; j < len1; index = ++j) {
          value = item[index];
          thisItem[object.headers[index]] = value;
        }
        result.push(thisItem);
      }
    }
    return result;
  };

  module.exports = {
    inflate: inflate,
    deflate: deflate
  };

}).call(this);
